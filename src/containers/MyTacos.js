import React from "react";
// import styled from "styled-components";
import TacoList from "../ui/TacoList";

function MyTacos({ tacos }) {
  return <TacoList tacos={tacos} />;
}

export default MyTacos;
